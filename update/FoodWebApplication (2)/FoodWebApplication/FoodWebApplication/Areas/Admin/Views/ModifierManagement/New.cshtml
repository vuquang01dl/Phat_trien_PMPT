@{
    Layout = "~/Areas/Admin/Views/Shared/_LayoutPage.cshtml";
}
@model DTO.Modifier.ModifierDTO
<div class="menu-management">
    @await Html.PartialAsync("_breadcrumb", new Dictionary<string, string> { { "A", "Homepage" }, { "B", "Modifier magement" }, { "C", "Thêm mới topping" } }, new ViewDataDictionary(ViewData) { { "mHref", Url.Action("Index", "ModifierManagement", new { area = "Admin" }) }, { "mShowButton", "true" } })

    @using (Html.BeginForm("New", "ModifierManagement", FormMethod.Post, new { @id = "form-create", @class = "form-horizontal needs-validation", @enctype = "multipart/form-data" }))
    {
        @await Html.PartialAsync("_Form", Model)
    }
</div>
